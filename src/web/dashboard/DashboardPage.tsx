import InternalNav from '../lib/InternalNav';

// Track D — SPEC §7: analytics dashboard.
export default function DashboardPage() {
  return (
    <main className="page page-wide">
      <InternalNav />
      <p>Dashboard — not built yet.</p>
    </main>
  );
}
