import InternalNav from '../lib/InternalNav';

// Track A — SPEC §5: version management page.
export default function AdminPage() {
  return (
    <main className="page page-wide">
      <InternalNav />
      <p>Admin — not built yet.</p>
    </main>
  );
}
