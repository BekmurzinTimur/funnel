import type { FastifyInstance } from 'fastify';

// Track A — SPEC §5: /api/admin/versions (list, publish, get, activate).
export default async function adminRoutes(_app: FastifyInstance): Promise<void> {}
