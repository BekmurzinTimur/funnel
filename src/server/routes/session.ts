import type { FastifyInstance } from 'fastify';

// Track A — SPEC §4: POST /api/session, /api/session/answer, /api/session/back.
export default async function sessionRoutes(_app: FastifyInstance): Promise<void> {}
